{
  "description": "Generic Streamable HTTP MCP example. Codex CLI uses config.toml as documented in README.md.",
  "mcpServers": {
    "investment-portfolio-tracker": {
      "transport": "streamable_http",
      "url": "http://127.0.0.1:8000/mcp/sse",
      "headers": {
        "Authorization": "Bearer ${IPT_MCP_PAT}"
      }
    }
  }
}
